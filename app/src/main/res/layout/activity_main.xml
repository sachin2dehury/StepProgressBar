<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".MainActivity">

    <include layout="@layout/layout_pager" />

    <github.sachin2dehury.stepprogressbar.SegmentedProgressBar
        android:id="@+id/spb"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_margin="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:segmentBackgroundColor="@android:color/holo_blue_bright"
        app:segmentCornerRadius="8dp"
        app:segmentMargins="@dimen/default_segment_margin"
        app:segmentSelectedBackgroundColor="@android:color/black"
        app:segmentSelectedStrokeColor="@android:color/black"
        app:segmentStrokeColor="@android:color/black"
        app:segmentStrokeWidth="0dp" />

</androidx.constraintlayout.widget.ConstraintLayout>